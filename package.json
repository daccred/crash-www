{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
       "preinstall": "npx only-allow yarn",
       "build": "yarn lerna run build",
       "test": "yarn lerna run test"
  },
  "devDependencies": {
    "lerna": "^8.1.3",
    "only-allow": "^1.2.1"
  }
}
